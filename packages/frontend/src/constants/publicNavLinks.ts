import { ROUTER_KEYS } from '~shared/keys';
import { NavLinks } from '~typings/auth.types';

const publicNavLinks: NavLinks = [{ title: 'About', path: ROUTER_KEYS.ABOUT }];

export default publicNavLinks;
