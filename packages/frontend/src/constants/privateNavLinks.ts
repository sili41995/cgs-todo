import { ROUTER_KEYS } from '~shared/keys';
import { NavLinks } from '~typings/auth.types';

const privateNavLinks: NavLinks = [
	{ title: 'Todos', path: ROUTER_KEYS.DASHBOARD },
];

export default privateNavLinks;
